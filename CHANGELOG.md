# Changelog

## 0.4.0

- Independent, guarded Python/FFmpeg artifact checks for the two narrated-video SOPs; full-text mapping, pilot, real PCM, subtitle coverage, final hash binding and stale-cover detection.
- Awaiting-user-review candidates, same-run revisions and bounded repair; failed runs remain governed.
- Lossless correction of the exact singleton item-array error; no arbitrary type coercion.
- Event-generated report exports and bounded archives of prior runs.
- Anonymized state regressions and real synthetic-media tests; no user session/media committed, no live-model improvement claim.

## 0.3.0

- Reject blank/empty evidence placeholders and unknown gate fields.
- Add non-mutating evidence preflight (`check`) and bounded format-only repairs.
- Record canonical foreground command receipts; verify call ID, command fingerprint and exit zero at five software-delivery gates.
- Preserve blocked runs and expose human resume/cancel controls; prevent model cancellation/restart from resetting gate budgets.
- Invalidate downstream accepted evidence on backtracking; failed evidence no longer flows forward.
- Expose run reports, explicit gatePassed/nextAction and bounded recent-evidence context.
- Keep automatic routing, all 19 SOPs, exports and old pinned run definitions.
- Add synthetic source-audit and mocked Host regressions; no claim of live-model benchmark improvement.

## 0.2.0

Automatic task routing and 19 starter SOPs.

## 0.1.1

Export the Web client bundle required for DSH composition.
